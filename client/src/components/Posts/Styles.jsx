const mainContainerStyles = {
  display: 'flex',
  alignItems: 'center',
};

const smMarginStyles = {
  m: 1, // Equivalent to theme.spacing(1)
};

const actionDivStyles = {
  textAlign: 'center',
};

export { mainContainerStyles, smMarginStyles, actionDivStyles };
